node-ip: "{{ k3s_node_ipv6 }}"
cluster-cidr: "{{ k3s_cluster_ipv6_cidr }}"
service-cidr: "{{ k3s_service_ipv6_cidr }}"
cluster-dns: "{{ k3s_cluster_dns_ipv6 }}"
flannel-iface: "{{ k3s_flannel_interface }}"
flannel-ipv6-masq: true
secrets-encryption: true
disable:
  - local-storage
  - metrics-server
  - servicelb
  - traefik
