proxmox_api_token          = "ansible@pam!ansible=<token-from-vault>"
suburban_proxmox_api_token = "ansible@pam!ansible=<token-from-suburban-vault>"
# proxmox_endpoint          = "https://[3d06:bad:b01::254]:8006/"      # default; override only if host IP changes
# suburban_proxmox_endpoint = "https://[3d06:bad:b01:200::1]:8006/"    # default; override only if host IP changes
