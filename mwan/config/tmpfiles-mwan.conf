# The ifmgr daemon's persistent state directory. The unit cannot use
# StateDirectory=, because that orders it after systemd-tmpfiles-setup.service
# and the daemon runs before the coldplug trigger that runs before it. The
# owner and mode match what StateDirectory= produced.
d /var/lib/mwan 0755 root root -
