# Generated by Ansible
#
# MWAN Watchdog environment (Proxmox-side)
# Sourced by /usr/local/bin/mwan-watchdog (Go binary)

# MWAN VM configuration
MWAN_VMID={{ mwan_vmid }}

# Watchdog behavior
DEPLOY_WINDOW_MINUTES={{ mwan_watchdog_deploy_window_minutes | default(30) }}
CONNECTIVITY_TIMEOUT_SECONDS={{ mwan_watchdog_connectivity_timeout | default(60) }}
CHECK_INTERVAL_HEALTHY={{ mwan_watchdog_check_interval_healthy | default(30) }}
CHECK_INTERVAL_DEGRADED={{ mwan_watchdog_check_interval_degraded | default(10) }}
ALERT_COOLDOWN_SECONDS={{ mwan_watchdog_alert_cooldown | default(300) }}

# Logging
LOG_FILE={{ mwan_watchdog_log_file | default('/var/log/mwan-watchdog.log') }}
LOG_JSON_FILE={{ mwan_watchdog_json_log_file | default('/var/log/mwan-watchdog.jsonl') }}
ROLLBACK_STATE_FILE={{ mwan_watchdog_state_file | default('/run/mwan-rollback.state') }}

# Email notifications
ALERT_EMAIL={{ mwan_email_recipient }}
SMTP2GO_API_KEY={{ mwan_smtp2go_api_key }}
# quiet | normal (default) | verbose
EMAIL_VERBOSITY={{ mwan_email_verbosity | default('normal') }}

# MWAN Agent gRPC (TCP management; fallback when vsock unavailable)
MWAN_AGENT_TCP_ADDR=[3d06:bad:b01::113]:50052

# Proxmox REST API (used by Go watchdog for guestExec fallback)
PVE_BASE_URL=https://127.0.0.1:8006/api2/json
PVE_NODE={{ mwan_proxmox_node }}
PVE_TOKEN_ID={{ mwan_watchdog_pve_token_id | default('root@pam!watchdog') }}
PROXMOX_API_TOKEN={{ mwan_watchdog_pve_token_secret }}

# Network config file (WAN interfaces, ping targets)
MWAN_NETWORK_CONFIG=/etc/mwan-watchdog/network.toml
